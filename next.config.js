/** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
//   swcMinify: true,
// };
const nextConfig = {
  output: 'export',
  distDir: 'dist',
}

module.exports = nextConfig;
